<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Pending Reviews Page</title>
    <link href="/util.css" rel="stylesheet"/>
</head>

<body>
{{>util/header}}

<section>
    <h1 style="font-size: 1.5rem; font-weight: 400; margin: 2rem 0 1rem 1rem; user-select: none;">Review</h1>
</section>

<div class="layout" style="display: flex; align-items: flex-start;">
    <aside style="display: flex; flex-direction: column; gap: 0.5rem; margin-left: 1rem;">
        {{#publicationsSidebar}}
            <a data-type="sidebar-button" data-id="{{id}}" href="/review/from-publication/{{id}}" style="text-decoration: none; color: inherit;">
                <label class="sidebar-button">
                    <span class="sidebar-button-title">{{title}}</span>
                </label>
            </a>
        {{/publicationsSidebar}}
    </aside>

    <section class="main-content" style="
        flex: 1;
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
        gap: 1rem;
        margin: 0 1.5rem 1rem 1.5rem;
        min-width: 0;
        overflow: hidden;
        margin-top: -2.7rem;
    ">

        <p style="font-size: 1.5rem; font-weight: 400; user-select: none;">Invited</p>

        {{#manuscripts}}
            <article data-id="{{id}}" class="yellow-box" style="width: auto; height: auto; max-width: 100%;">
                <p class="yellow-box-text">{{title}}</p>
                <br>

                <div class="authors" style="display: flex; flex-wrap: wrap;">{{#authors}} <button>{{.}}</button> {{/authors}}</div>

                <p style="font-weight: 100; font-size: 0.8rem; margin-bottom: 0.5rem; margin-top: 0.2rem;">{{publicationDate}}</p>
                <p style="font-weight: 100; font-size: 0.8rem">{{description}}</p>

                <footer class="yellow-box-buttons" style="margin-left: 1rem;  ">
                    <a href="#" class="m3-button purple-button" style="margin-left: 1rem;  box-shadow: none; width: auto; border-radius: 10rem; padding-block: 0.523rem; text-wrap: nowrap; background-color: var(--green); color: black; font-weight: normal; text-decoration: none;">Accept</a>
                </footer>
            </article>
        {{/manuscripts}}

        <br>

        <p style="font-size: 1.5rem; font-weight: 400; user-select: none;">Pending</p>

        {{#manuscripts}}
            <article data-id="{{id}}" class="yellow-box" style="width: auto; height: auto; max-width: 100%;">
                <p class="yellow-box-text">{{title}}</p>

                <div class="authors" style="display: flex; flex-wrap: wrap;">{{#authors}} <button>{{.}}</button> {{/authors}}</div>

                <p style="font-weight: 100; font-size: 0.8rem; margin-bottom: 0.5rem; margin-top: 0.2rem;">{{publicationDate}}</p>
                <p style="font-weight: 100; font-size: 0.8rem">{{description}}</p>

                <footer class="yellow-box-buttons" style="margin-left: 1rem;  ">
                    <a href="/review/{{id}}" class="m3-button purple-button" style="margin-left: 1rem;  box-shadow: none; width: auto; border-radius: 10rem; padding-block: 0.523rem; text-wrap: nowrap; font-weight: normal; text-decoration: none;">Review</a>
                </footer>
            </article>
        {{/manuscripts}}
    </section>
</div>
</body>

<style>
    .yellow-box-buttons {
        display: flex;
        gap: 0.5rem;
        margin-left: 1rem;
    }

    .yellow-box-buttons button {
        font-size: clamp(0.7rem, 1.2vw, 0.9rem);
        padding: clamp(0.3rem, 0.8vw, 0.5rem) clamp(0.6rem, 1.5vw, 1rem);
        border-radius: clamp(8px, 2vw, 10rem);
    }

    @media (max-width: 768px) {
        .yellow-box {
            padding: 1rem !important;
            margin: 0 !important;
        }

        .yellow-box-text {
            font-size: 0.9rem !important;
        }

        .yellow-box-buttons {
            margin-left: 0.5rem;
            gap: 0.4rem;
        }

        .yellow-box-buttons button {
            font-size: clamp(0.6rem, 1.8vw, 0.8rem) !important;
            padding: clamp(0.25rem, 0.6vw, 0.4rem) clamp(0.5rem, 1.2vw, 0.8rem) !important;
        }
    }

    @media (max-width: 480px) {
        .yellow-box {
            padding: 0.8rem !important;
        }

        .yellow-box-text {
            font-size: 0.8rem !important;
        }

        h1 {
            font-size: 1.2rem !important;
            margin: 1rem 0 0.5rem 1rem !important;
        }

        .authors {
            flex-direction: column;
        }

        .yellow-box-buttons {
            display: flex;
            flex-direction: column !important;
            gap: 0.3rem;
            margin-left: 0 !important;
        }

        .yellow-box-buttons button {
            font-size: clamp(0.5rem, 2.5vw, 0.7rem) !important;
            padding: clamp(0.2rem, 0.5vw, 0.3rem) clamp(0.4rem, 1vw, 0.6rem) !important;
            width: 100% !important;
        }
    }
</style>
</html>