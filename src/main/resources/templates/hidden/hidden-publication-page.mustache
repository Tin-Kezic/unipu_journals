<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hidden Publication</title>
    <link href="/util.css" rel="stylesheet"/>
</head>
<body>
{{>util/header}}

<div style="display: flex; justify-content: space-between; align-items: center; margin: 2rem 1.5rem 1rem 1.5rem;">
    <h1 style="font-size: 1.5rem; font-weight: 400;">
        publications
    </h1>
</div>

<section style="flex-direction: row; gap: 1rem; display: flex; flex-wrap: wrap; margin: 1rem 1.5rem;">
    {{#publications}}
        <div
            id="{{id}}"
            class="yellow-box"
            onclick="window.location='hidden/publication/{{id}}'"
            style="text-decoration: none; color: black; cursor: pointer;"
        >
            <p class="yellow-box-text">{{title}}</p>
            <div class="yellow-box-buttons">
                <button onclick="event.stopPropagation()" class="m3-button red-button" style="font-size: 0.8rem; width: fit-content; box-shadow: none; text-wrap: nowrap; padding: 0.5rem; border-radius: 1rem;">Delete permanently</button>
                <button onclick="event.stopPropagation()" class="m3-button outline-button" style="font-size: 0.8rem; width: fit-content; text-wrap: nowrap; padding: 0.5rem; border-radius: 1rem; background-color: white;">Restore</button>
            </div>
        </div>
    {{/publications}}
</section>

<dialog id="delete-publication-dialog" closedby="any">
    <p id="delete-publication-dialog-message"></p>
        <p>It contains <b>{{sectionsNumber}}</b> sections:</p>
        <br>
        {{#sections}}<b style="line-height: 1.5rem;"><p>{{.}}</p></b>{{/sections}}
       <br>
        <div style="display: flex; justify-content: space-between">
            <button id="delete-publication-dialog-negative" class="m3-button outline-button">cancel</button>
            <div style="display: flex; justify-content: flex-end">
                <button class="m3-button red-button" style="padding-inline: 1.2rem;">delete</button>
            </div>
        </div>
</dialog>
<script>
    const deletePublicationDialog = document.getElementById('delete-publication-title-dialog');
    const deletePublicationTitleDialogMessage = document.getElementById('delete-publication-title-dialog-message');
    document.querySelectorAll('button[data-type="edit-button"]').forEach(button => {
        button.addEventListener('click', () => {
            deletePublicationTitleDialogMessage.innerHTML = `edit title of <b>${button.dataset.title}</b>`;
            currentId = button.dataset.id;
            deletePublicationTitleDialog.showModal();
        });
    });
    document.getElementById('delete-publication-title-dialog-negative').addEventListener('click', () => deletePublicationTitleDialog.close());
    document.getElementById('delete-publication-title-dialog-form').addEventListener('submit', async (event) => {
        event.preventDefault();
        const newPublicationTitle = document.querySelector('input[name="delete-publication-title-dialog-input"]').value;
        const response = await fetch(`/api/publication/${currentId}/delete-title?title=${encodeURIComponent(newPublicationTitle)}`, { method: 'PUT' });
        if(!response.ok) { alert('failed to delete publication title'); return; }
        location.reload(true);
    });
</script>

<div class="overlay">
    <div class="dialog">
        <p>Restore <b>Acoustics</b> publication?</p>
        <p>It contains <b>9</b> sections:</p>
        <br>
        <b style="line-height: 1.5rem;"><p>Archaeoacoustics</p>
            <p>Aeroacoustics</p>
            <p>Bioacoustics</p>
            <p>Acoustic signal processing</p>
            <p>Architectural acoustics</p>
            <p>Electroacoustics</p>
            <p>Environmental noise and soundscape</p>
            <p>Musical acoustics</p>
            <p>Bioacoustics 2</p></b>
        <br>
        <div style="display: flex; justify-content: space-between">
            <button class="m3-button outline-button">cancel</button>
            <div style="display: flex; justify-content: flex-end">
                <button class="m3-button purple-button" style="padding-inline: 1rem; box-shadow: none;">restore</button>
            </div>
        </div>
    </div>
</div>
</body>

<style>
    .m3-button:active {
        transform: none;
        box-shadow: none !important;
    }
</style>
</html>